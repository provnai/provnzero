{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "provnzero-proxy",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "env": [
      {
        "key": "PORT",
        "value": "3001"
      },
      {
        "key": "OPENAI_BASE_URL",
        "value": "https://api.openai.com/v1"
      },
      {
        "key": "OPENAI_API_KEY",
        "value": "",
        "description": "Required to authenticate outbound traffic"
      }
    ]
  }
}
